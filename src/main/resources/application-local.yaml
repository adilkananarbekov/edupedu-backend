spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  url: ${APP_URL:http://localhost:8080}
  cors:
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:*,http://127.0.0.1:*,http://136.116.64.6:*,https://136.116.64.6:*}
